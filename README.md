# OpenCV Drawing Tool 🎨

A simple, menu-driven OpenCV project that allows users to draw **lines, rectangles, circles, and text** on an image using console input.  
Each drawing option refreshes the image so shapes can be previewed **independently or interactively**.

---

## 🚀 Features

- Draw Line
- Draw Rectangle
- Draw Circle
- Add Text to Image
- Image refresh for each option
- Keyboard-controlled preview
- Save edited image
- Simple console-based menu system

---

## 🛠️ Technologies Used

- Python
- OpenCV (`cv2`)
- NumPy (implicit through OpenCV)

---

## 📸 How It Works

1. User provides the path of an image
2. A menu appears with drawing options
3. Selected shape is drawn on a **fresh copy** of the image
4. Image is displayed using OpenCV
5. Press any key to close the window
6. Option to save the image
7. Program continues until user selects Exit

---

## ▶️ Usage

### 1️⃣ Install Dependencies
bash
pip install opencv-python

### 2️⃣ Run the Program
bash
python main.py
